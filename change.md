I have a Python multi-object tracking project (OCSI) with a bug: the behaviour-feedback mechanism is fully implemented in the tracker (ocsi/identity/tracker.py, methods _behaviour_terms and _maybe_update_behaviour) and consumes Detection.behaviour_embedding and Detection.activity_probs, but nothing in the codebase ever populates those two fields. I confirmed this by grepping the entire codebase for calls to BehaviourRecognizer.observe() (defined in ocsi/behaviour/recognizer.py) and found zero call sites. As a result, _behaviour_terms() always returns None (it bails out immediately when any detection has behaviour_embedding is None), so the behaviour-feedback stage of my experiments has never actually been exercised — it silently produces identical output to the memory-only stage every time.

Your task: wire the existing PrototypeBehaviourRecognizer (in ocsi/behaviour/recognizer.py) into the MOT17 experiment pipeline (ocsi/experiments/mot17_tracking.py), so that behaviour observations are actually computed and attached to detections before they reach tracker.update().

Specifically:

Find where Detection objects are constructed per frame in mot17_tracking.py (or wherever the per-frame detection list is built before being passed to OCSITracker.update()).
Add a per-track rolling window buffer for behaviour features, separate from the existing appearance gallery. For each confirmed/active track, maintain the last N per-frame feature vectors (N = PrototypeBehaviourRecognizer's min_window, default 8). For the feature vector, reuse the same embedding already computed for Re-ID (the existing Detection.embedding field) as a placeholder input — this is not meant to be a real action-recognition signal yet, it's meant to validate that the plumbing works end-to-end. Note this clearly in a code comment so it isn't mistaken for a real behaviour signal later.
Instantiate a PrototypeBehaviourRecognizer once per sequence run, with some reasonable placeholder prototypes (e.g. np.random.randn(num_classes, embedding_dim) normalized, with num_classes and embedding_dim matching the Re-ID embedding dimension) — again, flagged clearly as a placeholder, not a trained model.
Each frame, for each track with a full window, call recognizer.observe(window). If it returns a non-None ActivityObservation, attach observation.probs to the corresponding Detection.activity_probs and observation.embedding to Detection.behaviour_embedding for the detection that matched that track in the previous frame's association (or for new detections, leave both fields None until a window is available).
Do not modify tracker.py, gate.py, or association.py — the consuming logic is already correct and tested; this task is purely about supplying real (even if placeholder) values to the fields those modules already expect.
Add a config flag (e.g. cfg.behaviour.enabled = True needs to actually be respected — confirm this flag already gates the new window-building code too, so behaviour computation is skipped entirely when disabled, for performance parity with the current baseline/memory stages).
Verification: after wiring this up, run the existing three-stage experiment (baseline, memory, feedback) on one MOT17 sequence and confirm that feedback's output (MOTA/IDF1/IDsw) is now numerically different from memory's output — even if the difference is small, this confirms the gate is finally receiving live data instead of silently no-op'ing. Report the exact before/after numbers for memory vs feedback on that one sequence as proof the fix worked.